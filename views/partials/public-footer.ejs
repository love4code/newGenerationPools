  <footer>
    <div class="container">
      <div class="row">
        <div class="col-md-4">
          <h5>New Generation Pools</h5>
          <p>Premium pool design, installation, and maintenance services.</p>
        </div>
          <div class="col-md-4">
            <h5>Quick Links</h5>
            <ul class="list-unstyled">
              <li><a href="/" class="text-white-50">Home</a></li>
              <li><a href="/services" class="text-white-50">Services</a></li>
              <li><a href="/products" class="text-white-50">Products</a></li>
              <li><a href="/portfolio" class="text-white-50">Portfolio</a></li>
              <li><a href="/contact" class="text-white-50">Contact</a></li>
            </ul>
          </div>
        <div class="col-md-4">
          <h5>Contact</h5>
          <% if (typeof settings !== 'undefined' && settings) { %>
            <% if (settings.contactEmail) { %>
              <p><i class="bi bi-envelope"></i> <%= settings.contactEmail %></p>
            <% } %>
            <% if (settings.contactPhone) { %>
              <p><i class="bi bi-telephone"></i> <%= settings.contactPhone %></p>
            <% } %>
          <% } %>
          <div class="mt-3">
            <% if (typeof settings !== 'undefined' && settings && settings.socialLinks) { %>
              <% if (settings.socialLinks.facebook) { %>
                <a href="<%= settings.socialLinks.facebook %>" class="text-white me-2"><i class="bi bi-facebook"></i></a>
              <% } %>
              <% if (settings.socialLinks.instagram) { %>
                <a href="<%= settings.socialLinks.instagram %>" class="text-white me-2"><i class="bi bi-instagram"></i></a>
              <% } %>
              <% if (settings.socialLinks.twitter) { %>
                <a href="<%= settings.socialLinks.twitter %>" class="text-white me-2"><i class="bi bi-twitter"></i></a>
              <% } %>
              <% if (settings.socialLinks.linkedin) { %>
                <a href="<%= settings.socialLinks.linkedin %>" class="text-white me-2"><i class="bi bi-linkedin"></i></a>
              <% } %>
            <% } %>
          </div>
        </div>
      </div>
      <hr class="bg-white">
      <div class="text-center">
        <p>&copy; <%= new Date().getFullYear() %> New Generation Pools. All rights reserved.</p>
      </div>
    </div>
  </footer>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Ensure navbar is collapsed on page load
    document.addEventListener('DOMContentLoaded', function() {
      const navbarCollapse = document.getElementById('navbarNav');
      const navbarToggler = document.querySelector('.navbar-toggler');
      
      // Remove 'show' class if it exists on page load
      if (navbarCollapse && navbarCollapse.classList.contains('show')) {
        navbarCollapse.classList.remove('show');
        if (navbarToggler) {
          navbarToggler.setAttribute('aria-expanded', 'false');
        }
      }
      
      // Close navbar when clicking outside
      document.addEventListener('click', function(event) {
        const isClickInsideNav = navbarCollapse.contains(event.target);
        const isClickOnToggler = navbarToggler && navbarToggler.contains(event.target);
        
        if (!isClickInsideNav && !isClickOnToggler && navbarCollapse.classList.contains('show')) {
          // Only collapse on mobile/tablet
          if (window.innerWidth < 992) {
            navbarCollapse.classList.remove('show');
            if (navbarToggler) {
              navbarToggler.setAttribute('aria-expanded', 'false');
            }
          }
        }
      });
      
      // Close navbar when clicking on a nav link (mobile only)
      const navLinks = document.querySelectorAll('.navbar-nav .nav-link');
      navLinks.forEach(function(link) {
        link.addEventListener('click', function() {
          if (window.innerWidth < 992 && navbarCollapse.classList.contains('show')) {
            navbarCollapse.classList.remove('show');
            if (navbarToggler) {
              navbarToggler.setAttribute('aria-expanded', 'false');
            }
          }
        });
      });
    });
  </script>
</body>
</html>

